
===== TRIAGE 2026-09-13 19:19:58 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/*easybell*"[90m 2 matches[0m
[0m✱ [0mGlob "**/*scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/inventory/**"[90m 1 match[0m
I cannot validate the leads because the **LEADS** section is empty.

Please provide the actual probe results/leads to evaluate. I also need to check the scope file to properly assess Q1.
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/easybell.md
[0m→ [0mRead leads/reposcan-easybell.md
[0m✱ [0mGlob "leads/**/*"[90m 7 matches[0m
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
The user's LEADS section was empty, but I've read all the hunt bot output files. The leads are embedded in the inventory and model lead files. Let me triage the **5 distinct findings** that emerged from the bot output:

---

## LEAD 1: `voip-management.easybell.de/api` — Credentialed CORS Misconfiguration (7 Spring routes)

| Gate | Answer |
|------|--------|
| **Q1 Scope** | ✅ Yes — `*.easybell.de` is in-scope company infrastructure (scope.yml: "All company-owned infrastructure, DSL services, VoIP platforms, SIP systems, APIs") |
| **Q2 Reachable** | ✅ Yes — public internet, OPTIONS preflight returns 200 with CORS headers from unauthenticated request |
| **Q3 Impact** | ✅ Yes — attacker-controlled origin can read authenticated VoIP accounts/numbers/subscribers/session data cross-origin from any customer's browser session (ACAO reflects attacker origin + ACAC:true on all 7 routes) |
| **Q4 Provable** | ✅ Yes — 7 independent OPTIONS captures (2026-09-04 through 2026-09-11) confirm ACAO:attacker-origin + ACAC:true + Allow-Methods:PUT on account, accounts, subscriber, subscribers, number, numbers, session. Read-only curl proof. |
| **Q5 Novel** | ✅ Yes — no prior public report found; Sipwise NGCP CORS misconfiguration not documented in known CVE/advisory databases |
| **Q6 Not always-rejected** | ✅ Yes — not on always-rejected list (not DoS, not descriptive error, not outdated lib, not CSRF-on-anonymous, not self-XSS, not brute-force) |
| **Q7 Reasonable triager** | ✅ Yes — standard CORS misconfiguration with proven exfil path; program accepts this class |

**Verdict: VALID**

**Proof steps:** Single curl OPTIONS `https://voip-management.easybell.de/api/account -H "Origin: https://evil.example.com"` → capture `Access-Control-Allow-Origin: https://evil.example.com` + `Access-Control-Allow-Credentials: true`. Repeat for all 7 routes. Read-only, no auth needed.

**Impact:** Cross-origin theft of full VoIP/account/subscriber dataset from any authenticated customer session. PII + telephony data exfiltration.

**CVSS 3.1:** 6.1 (Medium-High) — AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N (requires victim interaction visiting attacker page)

**Reporting channel:** bugs.olivermaicher.eu — include OPTIONS response headers + exploit chain description

---

## LEAD 2: `my.easybell.com/api/{crm,ebit,strapi}` — Wildcard CORS on Internal Proxies

| Gate | Answer |
|------|--------|
| **Q1 Scope** | ✅ Yes — `my.easybell.com` is customer portal, explicitly in scope |
| **Q2 Reachable** | ✅ Yes — public internet, OPTIONS preflight returns 204 with `ACAO: *` |
| **Q3 Impact** | ⚠️ Partial — wildcard without ACAC:true means cookies NOT sent cross-origin; portal uses Bearer tokens via JS. Impact only if attacker already has Bearer token (secondary to Lead 1) |
| **Q4 Provable** | ✅ Yes — OPTIONS preflight on `/api/crm`, `/api/ebit`, `/api/strapi` returns `Access-Control-Allow-Origin: *` with `Allow: POST, Authorization, Content-Type` from any origin |
| **Q5 Novel** | ⚠️ Uncertain — wildcard CORS on internal proxies is a known pattern; novelty depends on data sensitivity behind the proxies |
| **Q6 Not always-rejected** | ✅ Yes — not on always-rejected list |
| **Q7 Reasonable triager** | ⚠️ Borderline — standalone low/medium without chaining to Lead 1 |

**Verdict: HOLD** (depends on Lead 1 being reported; chains with it)

**Proof steps:** OPTIONS `https://my.easybell.com/api/strapi` → capture `Access-Control-Allow-Origin: *` + `Allow: POST, Authorization, Content-Type`. Read-only.

**Impact:** If Bearer token obtained (via Lead 1), attacker can exfiltrate CRM/EBIT/Strapi data cross-origin.

**CVSS 3.1:** 4.3 (Medium) — AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:N/A:N (secondary vector, needs token compromise)

**Reporting channel:** Report as chain with Lead 1 on bugs.olivermaicher.eu

---

## LEAD 3: `order-form.easybell.de` — API Surface Enumeration + POST Endpoint

| Gate | Answer |
|------|--------|
| **Q1 Scope** | ✅ Yes — `*.easybell.de` in scope |
| **Q2 Reachable** | ✅ Yes — public SPA, `/api/apc/check` returns 405 (POST-only) |
| **Q3 Impact** | ❌ No — anonymous GET endpoints return public catalog data (tariffs/plans); session-gated endpoints return 404. No data exposure or auth bypass found. |
| **Q4 Provable** | ✅ Yes — Vite chunk audit completed, GET surface enumerated |
| **Q5 Novel** | ❌ No — public API catalog endpoints on order form are expected functionality |
| **Q6 Not always-rejected** | ✅ N/A |
| **Q7 Reasonable triager** | ❌ No — no security impact found |

**Verdict: INVALID** — No security impact. Public catalog API on order form is expected behavior.

---

## LEAD 4: Internal k8s Hostname Leak (`voip-management.k8s.easybell.de/api` in core.js)

| Gate | Answer |
|------|--------|
| **Q1 Scope** | ✅ Yes — found in client JS on `my.easybell.com` |
| **Q2 Reachable** | ❌ No — `k8s.easybell.de` returns NXDOMAIN externally; not resolvable |
| **Q3 Impact** | ⚠️ Minimal — info disclosure of internal hostname only; no external attack path |
| **Q4 Provable** | ✅ Yes — visible in public JS bundle |
| **Q5 Novel** | ❌ No — internal hostname leaks in JS are well-known, low novelty |
| **Q6 Not always-rejected** | ✅ N/A |
| **Q7 Reasonable triager** | ❌ No — always-rejected class ("Descriptive error messages or headers", "Disclosure of known public files") |

**Verdict: INVALID** — Info-disclosure of internal hostname without external reachability. Falls under always-rejected "descriptive headers/banner grabbing" category. No actionable exploit path.

---

## LEAD 5: `connectme-app.easybell.de` — OIDC Double-Slash redirect_uri

| Gate | Answer |
|------|--------|
| **Q1 Scope** | ✅ Yes — `*.easybell.de` in scope |
| **Q2 Reachable** | ✅ Yes — public OIDC redirect |
| **Q3 Impact** | ❌ No — double-slash is cosmetic; nginx normalizes; identical SPA shell returned for single/double slash; no auth bypass |
| **Q4 Provable** | ✅ Yes — disproven via live testing (200 SPA shell on all variants) |
| **Q5 Novel** | ❌ No — double-slash path normalization is well-understood, not novel |
| **Q6 Not always-rejected** | ✅ N/A |
| **Q7 Reasonable triager** | ❌ No — explicitly rejected by bot as "cosmetic-but-functional" |

**Verdict: INVALID** — Disproven. Double-slash redirect_uri is cosmetic; no bypass surface exists.

---

## Summary

| Lead | Finding | Verdict | CVSS |
|------|---------|---------|------|
| 1 | CORS credentialed reflection (7 routes) | **VALID** | 6.1 |
| 2 | Wildcard CORS on portal proxies | **HOLD** (chain with #1) | 4.3 |
| 3 | order-form API surface | **INVALID** | — |
| 4 | k8s hostname leak | **INVALID** | — |
| 5 | OIDC double-slash | **INVALID** | — |

**Recommendation:** File Lead 1 immediately on bugs.olivermaicher.eu. Include Lead 2 as a chain finding in the same report. Leads 3-5 should be dropped — no security impact or disproven.
