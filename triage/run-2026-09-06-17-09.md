
===== TRIAGE 2026-09-06 17:09:59 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m✱ [0mGlob "**/scope*"[90m 1 match[0m
[0m✱ [0mGlob "**/*.md"[90m 41 matches[0m
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/*probe*"[90m 2 matches[0m
I see the leads section appears to be empty. Let me check the repository for any relevant files.
[0m→ [0mRead probe-results.md
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/easybell.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
Let me read the lead files:
The "LEADS:" section in your message is empty. However, I can see the latest findings tracked across the lead files. Let me validate the **key consolidated findings** against the 7-Question Gate:

---

## LEAD 1: voip-cors-cred-exfil-v3 — Credentialed CORS on voip-management.easybell.de/api

| Q | Answer |
|---|--------|
| **Q1** In scope? | **YES** — `*.easybell.de` is explicitly in scope as "all company-owned infrastructure... APIs, customer portals" |
| **Q2** Attacker reachable? | **YES** — public, unauthenticated OPTIONS/GET confirms ACAO reflection + ACAC:true on 7 Spring routes (account, accounts, subscriber, subscribers, number, numbers, session) |
| **Q3** Real security impact? | **YES** — Cross-origin theft of authenticated customer VoIP/account/PII data. HTTP Basic realm `sipwisebroker.easybell.de` cached by browser; any evil.com page can `fetch()` with `credentials:'include'` |
| **Q4** Provable non-invasively? | **YES** — OPTIONS preflight with `Origin: https://evil.com` returns `ACAO: https://evil.com` + `ACAC: true`. GET returns same CORS headers on 401. HUMAN needed only for authenticated body confirmation |
| **Q5** Novel/unreported? | **Likely YES** — KB initially claimed singular routes locked to my.easybell.com (disproven). No prior public report found |
| **Q6** Not always-rejected? | **YES** — Not DoS, not brute-force, not clickjacking, not SSL, not descriptive headers |
| **Q7** Reasonable triager accept? | **YES** — Credentialed CORS with ACAC:true on authenticated API is a well-accepted HIGH finding |

### Verdict: **VALID**
- **Proof steps:** Single `curl -X OPTIONS -H "Origin: https://evil.com" -I https://voip-management.easybell.de/api/accounts` → confirm `Access-Control-Allow-Origin: https://evil.com` + `Access-Control-Allow-Credentials: true`. Repeat for all 7 routes.
- **Impact:** Cross-origin exfiltration of VoIP accounts, phone numbers, subscribers, session data from any authenticated customer session
- **CVSS 3.1:** 7.5 (High) — Network/Low/None/Changed/High/None)
- **Channel:** bugs.olivermaicher.eu (private program)

---

## LEAD 2: my-portal-api-proxy-wildcard — Wildcard CORS on my.easybell.com/api/* proxy

| Q | Answer |
|---|--------|
| **Q1** In scope? | **YES** — `my.easybell.com` is company-owned customer portal |
| **Q2** Attacker reachable? | **YES** — public; OPTIONS preflight on `/api/crm`, `/api/ebit`, `/api/strapi` returns ACAO:* from any origin |
| **Q3** Real security impact? | **PARTIAL** — Wildcard CORS (no ACAC:true) means cookies aren't sent, but Bearer tokens via JS are. If voip-management CORS exfil succeeds (Lead 1), this amplifies to CRM/Strapi data too |
| **Q4** Provable non-invasively? | **YES** — `curl -X OPTIONS -H "Origin: https://evil.com" -H "Access-Control-Request-Method: POST" https://my.easybell.com/api/crm` returns ACAO:* |
| **Q5** Novel? | **Unlikely on its own** — Wildcard CORS without ACAC:true is low-severity; amplification only if paired with Lead 1 |
| **Q6** Not always-rejected? | **YES** — Not on rejected list |
| **Q7** Reasonable triager accept? | **HOLD** — Standalone wildcard CORS without ACAC:true is informational at best. Only reportable as chain amplifier to Lead 1 |

### Verdict: **HOLD**
- Report as secondary finding only if Lead 1 is accepted. Standalone wildcard CORS without ACAC:true is too low impact.

---

## LEAD 3: my-portal-idor — Portal IDOR/BOLA on my.easybell.com

| Q | Answer |
|---|--------|
| **Q1** In scope? | **YES** — customer portal in scope |
| **Q2** Attacker reachable? | **YES** — login panel public |
| **Q3** Real security impact? | **YES** (if confirmed) — cross-tenant PII, billing, SIP data |
| **Q4** Provable non-invasively? | **NO** — Requires authenticated session to test. All probe results show only unauthenticated routes (login page, SPA shell) |
| **Q5** Novel? | **Unknown** — No PoC or evidence of actual IDOR, only hypothesis |
| **Q6** Not always-rejected? | **YES** |
| **Q7** Reasonable triager accept? | **NO** — Scanner hypothesis without evidence. Program rejects "reports resulting from automated scanning utilities without additional details or a POC" |

### Verdict: **INVALID**
- Hypothetical IDOR without any authenticated testing or PoC. Rejected per program's "scanner output alone is REJECTED" rule.

---

## LEAD 4: voip-api-v2-bola — BOLA on versioned VoIP API

| Q | Answer |
|---|--------|
| **Q1** In scope? | **YES** |
| **Q2** Attacker reachable? | **PARTIAL** — Spring backend confirmed via JSON 404 leak, but all 22+ probed resource names return nginx HTML 404. No live v2 endpoint discovered |
| **Q3** Real security impact? | **Hypothetical** — Would be High/Critical if a live endpoint existed |
| **Q4** Provable non-invasively? | **NO** — No live endpoint found to test. All resources return 404 |
| **Q5** Novel? | **Unknown** |
| **Q6** Not always-rejected? | **YES** |
| **Q7** Reasonable triager accept? | **NO** — No evidence of a vulnerable endpoint |

### Verdict: **INVALID**
- Speculative. No live v2 resource discovered after 22+ probes. Cannot prove vulnerability on non-existent endpoint.

---

## LEAD 5: internal-k8s-hostname-leak

| Q | Answer |
|---|--------|
| **Q1** In scope? | **YES** — info disclosure on in-scope asset |
| **Q2** Attacker reachable? | **YES** — JS bundle publicly fetchable |
| **Q3** Real security impact? | **NO** — `voip-management.k8s.easybell.de` confirmed NXDOMAIN (not externally resolvable). Internal-only hostname. Info disclosure only |
| **Q4** Provable? | **YES** — already confirmed passively |
| **Q5** Novel? | **YES** — leaked in core.js fallback URL |
| **Q6** Not always-rejected? | **YES**, but "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" and "Disclosure of known public files" are rejected |
| **Q7** Reasonable triager accept? | **NO** — Internal hostname in JS bundle with no external resolvability is information disclosure, not a security vulnerability. Program rejects banner grabbing/descriptive headers |

### Verdict: **INVALID**
- Info-only, no external attack surface. Falls under "descriptive error messages / banner grabbing" exclusion.

---

## Summary

| Lead | Verdict | Reason |
|---|---|---|
| voip-cors-cred-exfil-v3 | **VALID** | Credentialed CORS + ACAC:true on 7 authenticated API routes — HIGH impact, provable via OPTIONS |
| my-portal-api-proxy-wildcard | **HOLD** | Standalone wildcard CORS without ACAC:true is informational; only valid as chain amplifier |
| my-portal-idor | **INVALID** | Hypothetical, no PoC, scanner-only hypothesis |
| voip-api-v2-bola | **INVALID** | No live endpoint discovered; speculative |
| internal-k8s-hostname-leak | **INVALID** | Info disclosure, NXDOMAIN, excluded as banner grabbing |

**Only Lead 1 (voip-cors-cred-exfil-v3) is reportable.** Recommend filing via bugs.olivermaicher.eu with OPTIONS/GET curl PoC showing ACAO reflection + ACAC:true on all 7 routes.
